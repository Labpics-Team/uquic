{
  "schemaVersion": 1,
  "components": [
    { "id": "uquic-transport-library", "prefixes": [""] }
  ],
  "rules": [
    { "id": "no-product-imports", "kind": "forbidden-dependency", "from": [""], "to": ["external:go:github.com/Labpics-Team"], "rationale": "uquic is a product-agnostic transport library; Ametyst, Phantom and other product integrations depend on uquic, never the reverse." }
  ]
}
